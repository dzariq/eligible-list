service: test
edition: 1.0.0

provider:
  name: alibaba
  runtime: custom
  customName: php7.4
  memorySize: 128
  timeout: 10

functions:
  hello:
    handler: index.handler
    events:
      - http:
          path: /
          method: GET
